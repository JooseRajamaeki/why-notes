# Agent Skills

A collection of agent skills for automation tasks.

## Structure

```
skills/
  template/
    skill.py       - Template skill entry point
```

## Usage

Each skill lives in its own folder under `skills/` and contains a Python script as its entry point.
